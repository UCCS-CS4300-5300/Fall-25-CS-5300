[mutmut]
# Resilient Mutation Testing Configuration
# Use resilient_mutation_runner.py for best results

# Default: mutate permissions module (small, for testing)
paths_to_mutate=active_interview_backend/active_interview_app/permissions.py

# Test runner with Django - use bash -c with absolute paths
runner=bash -c "cd /home/ssm-user/Fall-25-CS-5300/active_interview_backend && python3 manage.py test active_interview_app.tests.test_permissions --verbosity=0 --failfast 2>/dev/null"

dict_synonyms=Struct, NamedStruct

# Tests directory
tests_dir=active_interview_backend/active_interview_app/tests/
